# BÁO CÁO — Phòng 9 · Agen Zalo
Kỳ: 2026-08-27 18h   ·   Nhánh: claude/agen-zalo-3780k7

## 1. LÀM ĐƯỢC TỪ LẦN BÁO TRƯỚC
*(Đây là báo cáo đầu tiên của phòng — tính từ lúc lập phòng.)*
- Dựng xong bot Zalo OA tra cứu đơn hàng: webhook kiểm tra chữ ký `X-ZEvent-Signature`, ack 200 trước rồi xử lý, chặn tin trùng `msg_id` (commit `0e27e22`).
- Tự làm mới access token Zalo và lưu lại refresh token mới (token Zalo chỉ dùng được 1 lần) — `src/zalo/client.js`.
- Công cụ tự lấy refresh token cho anh Sơn: `npm run token` (OAuth v4 + PKCE) + hướng dẫn 5 bước `docs/SETUP-ZALO.md` (commit `7989acb`).
- 53 test tự động pass, chạy offline. Đã thử toàn luồng OAuth với server Zalo giả — lấy token đúng.
- Vá lỗ hổng tuân thủ: bot bị cấm tự trả lời giá/thành phần/công dụng/liều, phải chuyển nhân viên (`src/agent/prompt.js`).

## 2. ĐANG LÀM
- Chờ anh Sơn chạy `npm run token` — chưa có token thì phòng không đẩy tiếp được việc gì.
- Nguồn đơn hàng vẫn là file mẫu `data/orders.json` (4 đơn giả), chưa nối hệ thống thật.
- Chưa có cách đọc bộ nhớ chung ở nhánh Tổng Chỉ Huy từ lúc bot đang chạy.

## 3. DỮ LIỆU MỚI TẠO RA  (để phòng khác dùng lại)
DATA UPDATE
- Loại dữ liệu: Trạng thái kết nối Zalo OA của Phòng 9
- Giá trị: **CHƯA KẾT NỐI.** Chưa có `ZALO_APP_ID` / `ZALO_APP_SECRET` / `ZALO_REFRESH_TOKEN` (không có file `.env`, không có `data/zalo-token.json`). Agent **chưa trả lời khách thật lần nào** — mới chạy thử với dữ liệu mẫu và server Zalo giả trong máy.
- Kỳ: 2026-08-27 18h
- Nguồn: kiểm tra trực tiếp repo nhánh `claude/agen-zalo-3780k7`
- Thời gian cập nhật: 2026-08-27 18:25 (giờ VN)
- Agent tạo: Phòng 9
- Trạng thái xác minh: Đã xác minh

## 4. ĐANG BỊ CHẶN — cần ai gỡ
- Chưa có App ID / App Secret / refresh token OA → cần: **anh Sơn** chạy `npm run token` theo `docs/SETUP-ZALO.md`. Phòng em không truy cập được tài khoản Zalo của anh, và không nhận secret qua chat.
- Chưa có API đơn hàng thật → cần: **anh Sơn hoặc phòng vận hành** cho endpoint tra đơn (chỉ cần 2 hàm: tra theo mã đơn, tra theo số điện thoại).
- Bot chưa đọc được `bo-nho-chung/san-pham/rich-coenzyme-q10.md` lúc chạy → cần: **Tổng Chỉ Huy** chốt cơ chế (xem mục 5).

## 5. CẦN DỮ LIỆU CỦA PHÒNG KHÁC
- Cần **Tổng Chỉ Huy** chốt cách phòng em đọc bộ nhớ chung khi bot đang chạy — để trả lời khách về giá/quy cách/công dụng. Luật ghi *"không phòng nào được tự ghi khác"*, nên phòng em **cố ý không chép số sang nhánh mình**: chép là tạo bản sao sẽ lệch khi Phòng 7 sửa. Đề xuất: bot đọc thẳng file từ nhánh Tổng Chỉ Huy lúc khởi động. Chờ duyệt mới làm.
- Cần **phòng vận hành** nguồn đơn hàng thật — để bot tra đúng đơn của khách thay vì dữ liệu mẫu.

## 6. PHÁT HIỆN MÂU THUẪN
- KHÔNG PHÁT HIỆN về số liệu — bot chưa đưa con số sản phẩm nào ra cho khách.
- Một điểm cần Tổng Chỉ Huy biết: phòng em được lập để **tra cứu đơn hàng**, nhưng luật kỳ này giao trách nhiệm tư vấn giá/quy cách/công dụng. Hai việc khác nhau. Tạm thời bot **chuyển nhân viên** mọi câu hỏi sản phẩm thay vì tự đoán — an toàn hơn là để bot nói sai giá 2.890.000đ hay nói quá công dụng.
