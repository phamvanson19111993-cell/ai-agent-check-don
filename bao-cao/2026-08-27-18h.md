# BÁO CÁO — Phòng 8 · Fanpage Pancake
Kỳ: 2026-08-27 18h   ·   Nhánh: claude/fanpage-pancake-auto-update-iy8fuu

## 1. LÀM ĐƯỢC TỪ LẦN BÁO TRƯỚC
- Dựng xong tool `pancake_export`: quét hội thoại Pancake, loại hội thoại đã gắn nhãn `CHỐT ĐƠN`, lấy SĐT, ghi CSV + đẩy Google Sheet (commit 21ef76f).
- Chuẩn hoá SĐT Việt Nam: `0913.351.394`, `+84…`, đầu số 11 số cũ → `0913351394`; 15 unit test pass.
- Gom 3 file Drive cũ thành 1 danh sách sạch: **794 số "Chưa chốt"**, đã lọc trùng và khôi phục số 0 đầu (`tools/gom_data.py`, commit a03303d).
- Tạo Google Sheet mới cho riêng page Phúc Thịnh — 248 số, SĐT giữ đúng số 0 đầu.
- Viết trình cài lịch chạy hằng ngày 1 lệnh: `caidat.sh` / `caidat.bat` (commit 9ee1ae9).

## 2. ĐANG LÀM
- Chờ API key Pancake của anh Sơn để chạy thật — toàn bộ code đã xong, chưa gọi được lần nào.
- Còn 1 file Drive lỗi cần xoá (bản mất số 0 đầu), ID `1nJJLT_Zg4UKP2UTtoshTijeEQywEe5wqCtw2zxp3hGM`.

## 3. DỮ LIỆU MỚI TẠO RA  (để phòng khác dùng lại)
DATA UPDATE
- Loại dữ liệu: Danh sách SĐT khách **chưa chốt đơn** (tên, SĐT, tình trạng, ngày, page, ghi chú)
- Giá trị: **794 số** tổng cộng — trong đó page BPT Tim Mạch Não Bộ (Phúc Thịnh): **248 số**; PHS: 197; PHS Sức Khoẻ Là Vàng: 164; PHS DILIM 3: 99; PHS DiLim 2: 39; PHS DILIM 1: 25; PHS DiLim 4: 20; PHS Dilim 5: 1; không rõ page: 1
- Kỳ: 27/4/2026 → **15/8/2026** (mốc mới nhất trong dữ liệu là 15/8, **không phải hôm nay**)
- Nguồn: 3 Google Sheet có sẵn trên Drive của anh Sơn — "SĐT chưa chốt Pancake - 3 tháng gần nhất" (`1_Sjw…kJjk`), "…3 tháng gần nhất (có Page)" (`1n3FF…Hvig`), "…tháng 5,4,3 và cũ hơn (có Page)" (`1R60h…uQ2s`). **Không phải kéo mới từ Pancake.**
- Thời gian cập nhật: 2026-08-27, đọc từ file Drive sửa lần cuối 20/8/2026
- Agent tạo: Phòng 8
- Trạng thái xác minh: Anh cung cấp, chưa đối chiếu — chưa đối chiếu ngược với Pancake vì chưa vào được Pancake
- Nơi lấy: Google Sheet "SĐT chưa chốt - Page Phúc Thịnh (21/7 - 15/8)" — `1BSR_Kia8knGPsTsGcniUTTTIbWS0UjVCJBCdmB66EkQ`

**Số đơn kéo trực tiếp từ Pancake kỳ này: 0.** Từ 16/8 đến nay: `CHƯA ĐỦ DỮ LIỆU`.

## 4. ĐANG BỊ CHẶN — cần ai gỡ
- Không có API key Pancake → chưa kéo được đơn nào từ Pancake → cần: **anh Sơn** (Pancake → Cấu hình → Cấu hình ứng dụng → Webhook & API Key).
- Máy chủ chạy em bị chặn mạng ra `pancake.vn` / `pages.fm` (HTTP 403) → em **không thể tự kéo từ phía em**, kể cả khi có key → cần: chạy tool trên máy anh Sơn.
- **Lịch chạy hằng ngày: CHƯA CÀI trên máy anh Sơn.** Trình cài đã viết xong (`./caidat.sh`, `caidat.bat`), nhưng phải anh Sơn chạy. Hiện tại vẫn là chạy tay.
- File Drive lỗi `1nJJLT…3hGM` chưa xoá được (kết nối Drive rớt lúc thao tác) → cần: anh Sơn xoá tay, hoặc để em thử lại.

## 5. CẦN DỮ LIỆU CỦA PHÒNG KHÁC
- Không có.

## 6. PHÁT HIỆN MÂU THUẪN
- Sheet cũ trên Drive lưu SĐT **mất số 0 đầu** (`913592043` thay vì `0913592043`) do Google đọc thành số. Bản mới của em đã sửa. Phòng nào đang dùng sheet cũ làm nguồn thì số sẽ lệch 1 ký tự đầu.
