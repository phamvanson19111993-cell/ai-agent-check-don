# BÁO CÁO — Phòng 9 · Agen Zalo
Kỳ: 2026-08-27 22h   ·   Nhánh: claude/agen-zalo-3780k7

## 1. LÀM ĐƯỢC TỪ LẦN BÁO TRƯỚC
- Gỡ xong mục 5 của đơn 18h: bot **đọc thẳng** hồ sơ sản phẩm + luật tuân thủ từ nhánh Tổng Chỉ Huy lúc chạy (`src/knowledge/sharedMemory.js`), **không chép số sang nhánh Phòng 9**.
- Đã chạy thử với dữ liệu thật: nạp đúng 2 file, 8.397 ký tự, có giá 2.890.000đ và cảnh báo warfarin trong prompt.
- Prompt tách 2 chế độ: đọc được hồ sơ → được tư vấn nhưng **chỉ bằng số trong hồ sơ**; không đọc được → tự lùi về chỉ tra đơn, chuyển nhân viên (`src/agent/prompt.js`).
- Nạp sẵn các ràng buộc của luật vào prompt: câu "không phải là thuốc", 4 câu hỏi sàng lọc trước khi tư vấn liều, chặn tư vấn khi khách nhắc thuốc chống đông/mang thai/đang điều trị/dị ứng.
- 60 test pass (thêm 7 test cho bộ nhớ chung).

## 2. ĐANG LÀM
- Vẫn chờ token Zalo OA — không có thì mọi thứ dừng ở mức chạy thử.
- Chưa nối nguồn đơn hàng thật (vẫn `data/orders.json`, 4 đơn giả).

## 3. DỮ LIỆU MỚI TẠO RA  (để phòng khác dùng lại)
DATA UPDATE
- Loại dữ liệu: Trạng thái kết nối Zalo OA của Phòng 9
- Giá trị: **CHƯA KẾT NỐI** — không có `.env`, không có `data/zalo-token.json`. Agent **chưa trả lời khách thật lần nào**, vẫn đang chạy thử. Không đổi so với kỳ 18h.
- Kỳ: 2026-08-27 22h
- Nguồn: kiểm tra trực tiếp repo nhánh `claude/agen-zalo-3780k7`
- Thời gian cập nhật: 2026-08-27 22:16 (giờ VN)
- Agent tạo: Phòng 9
- Trạng thái xác minh: Đã xác minh

DATA UPDATE
- Loại dữ liệu: Cơ chế phòng khác đọc bộ nhớ chung khi bot đang chạy
- Giá trị: Đọc trực tiếp bằng `git show <nhánh>:<file>`, cache 15 phút, đọc hỏng thì giữ bản cũ. Phòng nào cần dùng lại: `src/knowledge/sharedMemory.js` — copy được nguyên file, không phụ thuộc phần Zalo.
- Kỳ: 2026-08-27 22h
- Nguồn: mã nguồn Phòng 9, đã chạy thử với 2 file thật của Tổng Chỉ Huy
- Thời gian cập nhật: 2026-08-27 22:16 (giờ VN)
- Agent tạo: Phòng 9
- Trạng thái xác minh: Đã xác minh

## 4. ĐANG BỊ CHẶN — cần ai gỡ
- Chưa có App ID / App Secret / refresh token OA → cần: **anh Sơn** chạy `npm run token` (`docs/SETUP-ZALO.md`). Phòng em không truy cập được tài khoản Zalo của anh.
- Chưa có API đơn hàng thật → cần: **anh Sơn hoặc phòng vận hành** cho endpoint tra đơn.
- Cách đọc bộ nhớ chung: **đã tự gỡ**, không cần ai nữa. Nếu Tổng Chỉ Huy muốn cơ chế khác thì báo, xoá một file là xong.

## 5. CẦN DỮ LIỆU CỦA PHÒNG KHÁC
- Cần **phòng vận hành** nguồn đơn hàng thật — để bot tra đúng đơn của khách.
- Cần **Tổng Chỉ Huy** báo khi đổi đường dẫn file trong `bo-nho-chung/` — bot trỏ theo đường dẫn, đổi tên file là bot mất hồ sơ và tự câm về sản phẩm.

## 6. PHÁT HIỆN MÂU THUẪN
- KHÔNG PHÁT HIỆN về số liệu — bot lấy nguyên văn từ hồ sơ chuẩn, không tự sinh số.
- Nhắc lại điểm đã nêu kỳ 18h, chưa có phản hồi: phòng em lập ra để **tra đơn hàng**, nay gánh thêm tư vấn sản phẩm. Đã làm được, nhưng nếu Tổng Chỉ Huy muốn tách sang phòng CSKH riêng thì phần bộ nhớ chung dùng lại được nguyên vẹn.
