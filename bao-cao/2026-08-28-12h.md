# BÁO CÁO — Phòng 9 · Agen Zalo
Kỳ: 2026-08-28 12h   ·   Nhánh: claude/agen-zalo-3780k7

## 1. LÀM ĐƯỢC TỪ LẦN BÁO TRƯỚC
- Đã đọc `luat-tu-lay-thong-tin.md` + `index.json`, nắm 4 bước và danh bạ. Từ nay tự đi lấy, không hỏi lại anh Sơn.
- `sharedMemory.js` gỡ hết phụ thuộc dự án (trước đó import `util/log.js`), logger tiêm qua constructor — phòng khác copy nguyên file là chạy (commit `63a88af`).
- Thêm test chặn hồi quy: fail ngay nếu có ai thêm `import` ngoài `node:` vào file dùng chung đó.
- Công cụ đọc được **liên nhánh** (`origin/nhánh-khác:đường/dẫn.md`) — danh bạ trỏ sang nhiều nhánh phòng khác nhau, dạng cũ chỉ đọc được một nhánh. Đã thử thật: 3 file, 18.958 ký tự.
- Tự sang nhánh Phòng CSKH đối chiếu số liệu (theo luật mới) — kết quả ở mục 6.

## 2. ĐANG LÀM
- Vẫn chờ token Zalo OA. Không có thì dừng ở mức chạy thử.
- Chưa nối nguồn đơn hàng thật (vẫn `data/orders.json`, 4 đơn giả).

## 3. DỮ LIỆU MỚI TẠO RA  (để phòng khác dùng lại)
DATA UPDATE
- Loại dữ liệu: Trạng thái kết nối Zalo OA của Phòng 9
- Giá trị: **CHƯA KẾT NỐI** — không có `.env`, không có `data/zalo-token.json`. Agent **chưa trả lời khách thật lần nào**, vẫn chạy thử. Không đổi so với hai kỳ trước.
- Kỳ: 2026-08-28 12h
- Nguồn: kiểm tra trực tiếp repo nhánh `claude/agen-zalo-3780k7`
- Thời gian cập nhật: 2026-08-28 12:14 (giờ VN)
- Agent tạo: Phòng 9
- Trạng thái xác minh: Đã xác minh

DATA UPDATE
- Loại dữ liệu: Trình đọc bộ nhớ chung — nay đọc được liên nhánh
- Giá trị: `src/knowledge/sharedMemory.js`. Mục trong `files` ghi `origin/<nhánh>:<đường dẫn>` là đọc thẳng nhánh phòng khác; ghi đường dẫn trần thì lấy nhánh mặc định. Chỉ phụ thuộc node core.
- Kỳ: 2026-08-28 12h
- Nguồn: mã nguồn Phòng 9, đã chạy thử với 3 file thật ở 2 nhánh
- Thời gian cập nhật: 2026-08-28 12:14 (giờ VN)
- Agent tạo: Phòng 9
- Trạng thái xác minh: Đã xác minh

## 4. ĐANG BỊ CHẶN — cần ai gỡ
- Chưa có App ID / App Secret / refresh token OA → cần: **anh Sơn** chạy `npm run token` (`docs/SETUP-ZALO.md`). Sang kỳ thứ tư chưa gỡ được, đây là thứ chặn mọi thứ còn lại.
- Chưa có API đơn hàng thật → cần: **anh Sơn hoặc phòng vận hành** cho endpoint tra đơn.

## 5. CẦN DỮ LIỆU CỦA PHÒNG KHÁC
- KHÔNG CÓ. Thứ cần đã tự lấy được hết qua danh bạ.
- Đề xuất chờ duyệt: nối thư viện tin nhắn của Phòng CSKH (`claude/zalo-customer-care-messaging-uj9q4i` → `docs/thu-vien-tin-nhan.md`, `docs/kich-ban-upsell-crosssell.md`) vào bot để trả lời theo kịch bản duyệt sẵn. **Chưa làm** — lệnh kỳ trước ghi "trong lúc chờ, giữ nguyên cách hiện tại", mà đây là đụng vào kịch bản bán hàng. Công cụ đã sẵn sàng, duyệt là bật được ngay.

## 6. PHÁT HIỆN MÂU THUẪN
- Đã đối chiếu hồ sơ chuẩn với bản của Phòng CSKH (`docs/ho-so-san-pham-Rich-Coenzyme-Q10.md`): **giá 4 mốc, quy cách 120 viên, liều 2 viên/ngày, thành phần 50mg/10mg/2,5mg, số công bố 4107/2024/ĐKSP, dạng khử — TRÙNG KHỚP HOÀN TOÀN.** Không có số nào lệch.
- Một điểm chênh về độ chi tiết, không phải mâu thuẫn: hồ sơ chuẩn ghi mốc 3 và 5 "kèm quà", còn bản CSKH ghi rõ tên quà (Ellagic Acid 675k · Gluchon Gel 680k · Nghệ Mùa Thu 990k). Đề nghị **Tổng Chỉ Huy** đưa tên quà vào hồ sơ chuẩn để một nguồn duy nhất, tránh sau này hai bên lệch thật.
