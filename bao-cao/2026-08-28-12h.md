# BÁO CÁO — Phòng 6 · 🩺 Video content sức khoẻ
Kỳ: 2026-08-28 12h   ·   Nhánh: claude/ai-agent-health-video-content-rmedj9

## 1. LÀM ĐƯỢC TỪ LẦN BÁO TRƯỚC
- Chuẩn bị bộ soát thành công cụ dùng chung theo lệnh chung: kiểm chứng bằng cách giả lập máy không cài `anthropic` và xoá sạch biến khoá — 4 lệnh `check`, `list`, `plan`, `hooks` chạy tốt (commit `cc1568d`)
- Sửa một lỗi thật phát hiện khi kiểm: `generate` vỡ bằng traceback thay vì báo lỗi tử tế khi máy chưa cài SDK. Phòng khác tải repo về sẽ gặp ngay lỗi này.
- Thêm dấu `<!-- soat-tuan-thu: bo-qua -->` cho tài liệu tra cứu — file liệt kê từ cấm làm ví dụ trước đây bị chính bộ soát chặn oan
- Viết `docs/dung-chung-bo-soat.md`: cách lấy về, chạy, đọc mã thoát để cắm CI, 11 nhóm lỗi nó bắt được và **những gì nó không bắt được**
- Tự lấy bản giá mới nhất từ hồ sơ chuẩn, cập nhật **mốc 2 hộp** và tên quà tặng từng mốc vào `knowledge/products.json`

## 2. ĐANG LÀM
- Không có việc dở dang.

## 3. DỮ LIỆU MỚI TẠO RA
KHÔNG CÓ DỮ LIỆU MỚI. Kỳ này Phòng 6 chỉ tiêu thụ dữ liệu của phòng khác, không tạo ra số nào.

## 4. ĐANG BỊ CHẶN — cần ai gỡ
- Vẫn chưa quay được video nói giá → cần: **anh Sơn xác nhận bảng giá và điều kiện quà tặng**. Treo từ kỳ 18h ngày 27/08, đây là kỳ thứ tư.
- Không được nhắc 5 số giấy tờ đang hiện trên trang bán → cần: **anh Sơn gửi ảnh**. Cả 5 vẫn nằm trong `chua_co_nguon` của `index.json`.
- Chưa ai trả lời: **có được nêu dạng hoạt chất trong quảng cáo không**, khi hồ sơ công bố chỉ ghi tên hoạt chất chung → cần: anh Sơn hoặc đơn vị làm hồ sơ công bố

## 5. CẦN DỮ LIỆU CỦA PHÒNG KHÁC
- Cần **ảnh mặt hộp gốc Nhật thấy rõ chữ 還元型** từ Phòng 7 — làm B-roll cho video nói dạng khử
- Cần **số giữ chân giây thứ 5 của từng hook** từ Phòng Ads — hiện xếp hạng 10 hook bằng phán đoán
- Cần **câu khách hay hỏi nhất** từ Phòng CSKH — để viết đúng phần "ba bình luận hay gặp"

## 6. PHÁT HIỆN MÂU THUẪN
- **Bản sao dữ liệu của Phòng 6 đã lệch thật, đúng như luật cảnh báo.** Mốc 2 hộp (5.780.000đ, không kèm quà) được thêm ngày 27/08 mà Phòng 6 không biết; bảng giá trong `products.json` thiếu mốc này gần một ngày. Đã cập nhật và thêm mục `CANH_BAO_BAN_SAO` bắt buộc đối chiếu lại nguồn gốc trước mỗi lần quay video nói giá. **Đề nghị Tổng Chỉ Huy báo cho các phòng khác đang giữ bản sao giá tự kiểm lại.**
- **Câu cảnh báo trong hồ sơ chuẩn chưa cập nhật theo bảng.** Bảng giá đã có mốc 2 hộp, nhưng câu cảnh báo ngay dưới vẫn viết "Mốc 1–3–5 đều ra đúng 48.167đ/ngày" — thiếu mốc 2. Bảng đúng, câu chữ sót. Nhờ Tổng Chỉ Huy sửa cho khớp.
