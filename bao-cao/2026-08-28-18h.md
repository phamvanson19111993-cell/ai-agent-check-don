# BÁO CÁO — Phòng 9 · Agen Zalo
Kỳ: 2026-08-28 18h   ·   Nhánh: claude/agen-zalo-3780k7

## 1. LÀM ĐƯỢC TỪ LẦN BÁO TRƯỚC
- Đọc `luat-du-lieu-khach.md` (mới ra 09:04 hôm nay) rồi **tự soát nhánh mình**: không có token, khoá API hay `.env` nào lọt vào git, kể cả trong lịch sử.
- Siết dữ liệu mẫu cho khỏi nhầm là khách thật: `data/orders.json` thêm dòng `_doc` ghi rõ là dữ liệu giả, đổi tên thành dạng "Nguyễn Văn A", đổi số `0938887766` → `0901234569` cho cùng dải giả.
- `docs/SETUP-ZALO.md` thêm cảnh báo đỏ: repo công khai, lỡ commit `.env` là mất app secret vĩnh viễn, phải tạo lại secret key.
- 62 test vẫn pass sau khi sửa dữ liệu mẫu.

## 2. ĐANG LÀM
- Vẫn chờ token Zalo OA. Không có thì dừng ở mức chạy thử.
- Chưa nối nguồn đơn hàng thật (vẫn `data/orders.json`, 4 đơn giả).

## 3. DỮ LIỆU MỚI TẠO RA  (để phòng khác dùng lại)
DATA UPDATE
- Loại dữ liệu: Trạng thái kết nối Zalo OA của Phòng 9
- Giá trị: **CHƯA KẾT NỐI** — không có `.env`, không có `data/zalo-token.json`. Agent **chưa trả lời khách thật lần nào**, vẫn chạy thử. Không đổi sang kỳ thứ năm.
- Kỳ: 2026-08-28 18h
- Nguồn: kiểm tra trực tiếp repo nhánh `claude/agen-zalo-3780k7`
- Thời gian cập nhật: 2026-08-28 18:21 (giờ VN)
- Agent tạo: Phòng 9
- Trạng thái xác minh: Đã xác minh

DATA UPDATE
- Loại dữ liệu: Kết quả soát rò dữ liệu khách trên nhánh Phòng 9
- Giá trị: **SẠCH.** Không có tên/số/địa chỉ khách thật, không có token hay khoá API trong file đang theo dõi lẫn trong lịch sử git. Các số điện thoại trong repo đều thuộc dải giả `090123456x`.
- Kỳ: 2026-08-28 18h
- Nguồn: `git grep` toàn nhánh + quét lịch sử `git rev-list --all`, chạy 28/08 18h
- Thời gian cập nhật: 2026-08-28 18:21 (giờ VN)
- Agent tạo: Phòng 9
- Trạng thái xác minh: Đã xác minh

## 4. ĐANG BỊ CHẶN — cần ai gỡ
- Chưa có App ID / App Secret / refresh token OA → cần: **anh Sơn** chạy `npm run token` (`docs/SETUP-ZALO.md`). **Kỳ thứ năm liên tiếp.** Mọi thứ còn lại đã xong, chỉ thiếu token là bot chưa nói được với khách nào.
- Chưa có API đơn hàng thật → cần: **anh Sơn hoặc phòng vận hành** cho endpoint tra đơn.

## 5. CẦN DỮ LIỆU CỦA PHÒNG KHÁC
- KHÔNG CÓ.
- Đề xuất cũ vẫn treo (không phải chặn): nối thư viện tin nhắn Phòng CSKH vào bot. Chưa làm vì MT-15 chưa có kết luận — xem mục 6.

## 6. PHÁT HIỆN MÂU THUẪN
- **Đã đóng:** đề nghị kỳ 12h về tên quà tặng đã được nhận — hồ sơ chuẩn nay ghi rõ Ellagic Acid 675k · Gluchon Gel 680k · Nghệ Mùa Thu 990k. Một nguồn duy nhất, không còn lệch với bản CSKH.
- **Đang mở, không phải của em quyết:** MT-15 (vai trò Phòng 9 lệch) đã nằm trong `mau-thuan-dang-mo.md`, chờ anh Sơn. Em giữ nguyên cách hiện tại như lệnh.
- Ghi nhận để Tổng Chỉ Huy biết: nhánh `claude/telegram-duplicate-order-bot-ayyubm` vẫn còn tên + số điện thoại khách thật trong `README.md`, `bot.py`, `cai_dat.bat`, `chan_doan.command`. Luật ghi phòng đó hết hạn mức model nên chưa sửa được. Repo công khai nên đây là rò đang mở — phòng em sửa hộ được nếu Tổng Chỉ Huy giao.
