# Phòng 7 · Lady Page — 13h ngày 29/08

## 1. Giờ qua làm được gì
`17070ed` — **chuông báo đơn** `apps-script/chuong-don.gs`. Dán vào Apps Script của
BẢNG TRẢ LỜI, chạy `catChuong` một lần: khách bấm gửi là anh Sơn có thư ngay,
kèm Telegram nếu điền mã bot mới. Có `chayThu` để thử. Không nhét mã bot vào kho.

`2c26c17` — **hồ sơ quảng cáo Q10 qua được bộ soát luật**, 6 chỗ hỏng về 0.
Em bấm thử quy trình "Chay quang cao" trên máy chủ GitHub (máy đó vào được Facebook).
Lần 1 bị chặn ở bước soát luật. Đã sửa: ảnh quảng cáo trỏ vào file không tồn tại →
đổi sang ảnh có thật · khai số công bố 4107/2024/ĐKSP thành ô riêng và thêm vào bài
nhóm 1, 2 · thêm câu chống chỉ định vào cả ba nhóm · bỏ chữ cấm "duy nhất".
Lần 2: soát luật ĐẠT, bộ tự kiểm 27/27 ĐẠT.

## 2. Số mới nhất
Không đổi. Không có ảnh Ads mới trong chat từ 22h ngày 28/08 — **17 giờ không lượt xem**.
Bảng trả lời Biểu mẫu Q10 vẫn chưa được tạo. Bảng "Lady Page" 0 đơn.

## 3. Cải tiến giờ này
Hai việc ở mục 1. Không đụng trang, không đụng Pixel, không đụng giá.

## 4. Cần gỡ — CHỈ CÒN MỘT THỨ CHẶN VIỆC BẬT QUẢNG CÁO
**Bí mật kho `FB_TOKEN` chưa có.** Quy trình chạy tới bước cuối rồi dừng đúng ở đó:
"Thieu Bi mat kho FB_TOKEN".
→ github.com/phamvanson19111993-cell/ai-agent-check-don/settings/secrets/actions
→ New repository secret → Name: FB_TOKEN → dán mã → Add secret.
Token không nằm trong mã nguồn, không hiện trong nhật ký, Phòng 7 không đọc được.
Có nó là Phòng 7 bấm chạy được, không cần anh Sơn mở máy tính nữa.

Việc thứ hai (không chặn bật, nhưng chặn việc THẤY đơn): tạo bảng trả lời Biểu mẫu.
