# BÁO CÁO — Phòng 8 · Fanpage Pancake
Kỳ: 2026-08-29 18h   ·   Nhánh: claude/fanpage-pancake-auto-update-iy8fuu

> Thú nhận trước: em **bỏ 5 kỳ báo cáo** (27/08 22h, 28/08 12h·18h·22h, 29/08 12h).
> Lỗi của em, không phải không có việc. Đơn này gộp toàn bộ từ 27/08 18h đến nay.

## 1. LÀM ĐƯỢC TỪ LẦN BÁO TRƯỚC
- Nhận và nắm **luật tự lấy thông tin** (`bo-nho-chung/luat-tu-lay-thong-tin.md`) — từ nay tự sang nhánh phòng khác lấy, không hỏi anh Sơn thứ phòng khác đã có.
- Tách `pancake_export/phones.py` thành **công cụ dùng chung**: thêm hướng dẫn dùng, thêm CLI gọi thẳng `python3 phones.py "0913.351.394"` (commit 1a0997e).
- Khoá tính độc lập bằng **3 test**: không import gì trong gói Pancake · chỉ dùng thư viện chuẩn · copy riêng file sang thư mục khác vẫn chạy. Tổng 18 test, pass hết.
- Viết `docs/cong-cu-dung-chung-so-dien-thoai.md` cho Phòng 10, Phòng 11, Phòng Ads — kèm bảng các ca xử lý được và cách gọi.
- Kiểm tra lại đường mạng ra Pancake lúc 22h 27/08 và 18h 29/08: **vẫn chặn** (HTTP 000/403).

## 2. ĐANG LÀM
- Không có việc mới. Toàn bộ code đã xong, đang **đứng chờ API key Pancake** của anh Sơn — không có key thì không chạy tiếp được dòng nào.
- Còn tồn: 1 file Drive lỗi chưa xoá (ID `1nJJLT_Zg4UKP2UTtoshTijeEQywEe5wqCtw2zxp3hGM`).

## 3. DỮ LIỆU MỚI TẠO RA  (để phòng khác dùng lại)
DATA UPDATE
- Loại dữ liệu: **Công cụ chuẩn hoá SĐT Việt Nam** (không phải số liệu)
- Giá trị: `pancake_export/phones.py` — chạy độc lập, 0 thư viện ngoài; hướng dẫn tại `docs/cong-cu-dung-chung-so-dien-thoai.md`
- Kỳ: cập nhật 28/08/2026
- Nguồn: nhánh `claude/fanpage-pancake-auto-update-iy8fuu`, commit 1a0997e
- Thời gian cập nhật: 2026-08-28
- Agent tạo: Phòng 8
- Trạng thái xác minh: Đã xác minh — 18 test pass

**Số liệu đơn hàng: KHÔNG CÓ DỮ LIỆU MỚI kỳ này.**
- Kéo trực tiếp từ Pancake: **0 đơn** (chưa lần nào kéo được).
- Kho hiện có vẫn là **794 số chưa chốt**, mốc mới nhất **15/8/2026** — nguồn: 3 sheet cũ trên Drive của anh Sơn, đã báo ở đơn 27/08 18h. Không chép lại số sang đơn này.
- Từ 16/8 đến 29/8: `CHƯA ĐỦ DỮ LIỆU`.
- **Lịch chạy hằng ngày: CHƯA CÀI trên máy anh Sơn** — vẫn phải chạy tay.

## 4. ĐANG BỊ CHẶN — cần ai gỡ
- Không có API key Pancake → cần **anh Sơn** (Pancake → Cấu hình → Cấu hình ứng dụng → Webhook & API Key). Đây là nút chặn duy nhất còn lại.
- Máy chủ chạy em bị chặn mạng ra `pancake.vn` / `pages.fm` → em không kéo được từ phía em kể cả khi có key → cần **anh Sơn chạy `./caidat.sh` trên máy anh** (1 lệnh, đã làm sẵn).
- File Drive lỗi `1nJJLT…3hGM` chưa xoá được → cần anh Sơn xoá tay, hoặc để em thử lại kỳ tới.
- Em bỏ 5 kỳ báo cáo → đã tự sửa, không cần ai gỡ.

## 5. CẦN DỮ LIỆU CỦA PHÒNG KHÁC
- Không có.

## 6. PHÁT HIỆN MÂU THUẪN
- Không phát hiện thêm. Mâu thuẫn cũ **MT-14** (sheet Drive cũ mất số 0 đầu SĐT) đã vào sổ chung — cách khắc phục: chạy `normalize()` trước khi so khớp, đừng so chuỗi thô.
