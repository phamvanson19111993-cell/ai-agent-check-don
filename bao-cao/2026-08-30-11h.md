# Phòng 7 · Lady Page — 11h ngày 30/08

## 1. Giờ qua làm được gì
`cb563c7` — **dựng xong đường gia hạn quảng cáo**, nhưng CHƯA bấm được nút.
- `viec_gia_han` trong `thuc-thi.py`: chỉ kéo dài hạn, không bao giờ rút ngắn.
  Hết hạn rồi thì tính từ bây giờ. Không đụng `status` nên mẫu đang tắt vẫn nằm
  im; không đổi ngân sách/đối tượng/nội dung nên Facebook không học lại từ đầu.
  In hạn cũ, hạn mới, ngân sách và số tiền tối đa tiêu thêm trước khi hỏi.
- `lenh-gia-han.json` riêng — cố ý không dùng chung `lenh.json` vì trong đó có
  `tam_dung_het` và `bat`, chạy không người canh là hỏng to.
- `thuc-thi.py` thêm `--lenh <file>` (chặn đường dẫn lạ) và `--khong-hoi`
  (bắt buộc đi kèm `--lam`).
- `.github/workflows/gia-han-quang-cao.yml` — mặc định XEM TRƯỚC, không đổi gì.
  Muốn đổi thật phải tự tay gõ `GIA HAN THAT`.
- Thử: `thu-gia-han.py`, **12 mục, đạt hết**, dùng Facebook giả lập.

## 2. Số mới nhất — không đổi
Đơn 10h13 (số lạ, 1 hộp) vẫn chờ anh Sơn gọi xác minh. Không có dòng nào mới.

## 3. HAI CHỖ CHẶN — em không tự vượt được
1. **Quy trình chưa có trên nhánh `main`.** GitHub chỉ cho bấm Run workflow khi
   file nằm trên nhánh mặc định. Gọi API trả 404. Nhánh `main` hiện chỉ có
   `bao-cao-telegram.yml`. Em **không được phép** đẩy sang nhánh khác nhánh
   được giao.
2. **Không biết kho có `FB_TOKEN` chưa.** Cổng mạng chặn đường đọc danh sách
   Bí mật kho, nên em không kiểm được. Không có nó thì quy trình dừng ngay
   bước đầu.

## 4. Cần gỡ
| | Việc | Ai gỡ |
|---|---|---|
| 1 | **Gia hạn ngay trong Ads Manager** — 30 giây, không chờ gì | anh Sơn |
| 2 | Cho phép em mở PR đưa quy trình sang `main` + thêm `FB_TOKEN` | anh Sơn |
| 3 | Gọi 0913131546 xác minh đơn 10h13 | anh Sơn |
