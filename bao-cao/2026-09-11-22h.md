# BÁO CÁO — Phòng 8 · Fanpage Pancake
Kỳ: 2026-09-11 22h   ·   Nhánh: claude/fanpage-pancake-auto-update-iy8fuu

> Đơn trước của em là kỳ 29/08 18h. Từ đó đến nay em **bỏ nhiều kỳ báo cáo** —
> lỗi của em. Đơn này gộp cả khoảng trống 30/08 → 11/09.

## 1. LÀM ĐƯỢC TỪ LẦN BÁO TRƯỚC
- **Không có việc mới.** Từ 29/08 đến nay không có yêu cầu mới nào từ anh Sơn và không có dữ liệu mới vào.
- Kiểm tra lại đường mạng ra Pancake hôm nay (11/09): **vẫn chặn** (HTTP 000).
- Chạy lại bộ test: **18/18 pass** — công cụ vẫn hoạt động, không hỏng theo thời gian.

## 2. ĐANG LÀM
- Không có việc đang dở. Toàn bộ code xong từ 28/08, đứng chờ **API key Pancake**.
- Còn tồn: file Drive lỗi `1nJJLT_Zg4UKP2UTtoshTijeEQywEe5wqCtw2zxp3hGM` chưa xoá (kết nối Drive chập chờn mỗi lần em định thao tác).

## 3. DỮ LIỆU MỚI TẠO RA  (để phòng khác dùng lại)
KHÔNG CÓ DỮ LIỆU MỚI.

- Kéo trực tiếp từ Pancake kỳ này: **0 đơn**. Từ đầu đến nay chưa kéo được lần nào.
- Kho cũ vẫn là **794 số chưa chốt**, mốc mới nhất **15/8/2026** — nguồn: 3 sheet Drive của anh Sơn, đã khai ở đơn 27/08 18h. Không chép lại sang đơn này.
- Từ 16/8 đến 11/9: `CHƯA ĐỦ DỮ LIỆU`.
- **Lịch chạy hằng ngày: CHƯA CÀI trên máy anh Sơn** — vẫn phải chạy tay.
- Công cụ dùng chung `pancake_export/phones.py` vẫn nguyên trạng, nguồn: nhánh này, commit 1a0997e.

## 4. ĐANG BỊ CHẶN — cần ai gỡ
- **Không có API key Pancake** → đường ống đứng im 15 ngày. Cần **anh Sơn** (Pancake → Cấu hình → Cấu hình ứng dụng → Webhook & API Key).
- Máy chủ em bị chặn mạng ra `pancake.vn`/`pages.fm` → không kéo được từ phía em kể cả khi có key. Cần **anh Sơn chạy `./caidat.sh` trên máy anh** (1 lệnh, đã làm sẵn từ 27/08).
- Dữ liệu "chưa chốt" mà Phòng Data Center và Phòng Tài chính đang dùng **đã cũ gần 1 tháng**. Ai đang tính toán trên đó cần biết mốc dừng là 15/8.
- Em bỏ nhiều kỳ báo cáo → lỗi của em, không cần ai gỡ.

## 5. CẦN DỮ LIỆU CỦA PHÒNG KHÁC
- Không có.

## 6. PHÁT HIỆN MÂU THUẪN
- Không phát hiện thêm. **MT-14** (sheet Drive cũ mất số 0 đầu SĐT) vẫn đúng như đã ghi — khắc phục bằng `normalize()` trước khi so khớp.
