# Phòng 7 · Lady Page — 03h ngày 13/09

## 1. Giờ qua làm được gì
Push báo cáo 02h (`c872eb7`). Soi tiếp chặng rơi nặng nhất — bắt đầu điền 11 →
gửi đơn 2 (82%).

## 2. Số mới nhất — đọc lúc 03h19, KHÔNG ĐỔI
Bảng đơn vẫn **21 dòng, dòng cuối 30/08 11:55:16** → ngày thứ **15**. Không có
số quảng cáo mới.

## 3. CẢI TIẾN — gỡ mìn trong trang
- Sửa gì: thẻ `#ok-msg` có sẵn câu **"Đây là bản demo giao diện nên thông tin
  chưa được gửi đi đâu cả"**. Thay bằng câu xác nhận thật.
- Vì sao: khách hiện **không** thấy câu đó (hàm `duPhong()` luôn ghi đè trước khi
  hiện), nhưng chỉ cần một đường nào đó hiện khối này mà không qua `duPhong()`
  là khách vừa đặt 17 triệu sẽ đọc được. Không đáng để nằm trong trang công khai.
- Đo bằng: không đo được bằng số — đây là chặn rủi ro, không phải tăng tỷ lệ.

## 4. HAI ĐIỀU CẦN ANH SƠN QUYẾT (mới)
- **Số "bắt đầu điền" đang bị thổi lên.** `InitiateCheckout` bắn ở lần `input`
  đầu tiên, mà bấm chọn mốc hộp cũng tính. Người chỉ bấm thử giá, chưa gõ chữ
  nào, vẫn được đếm. Vậy rơi 82% có thể nhẹ hơn thật, còn chặng trước nặng hơn.
  Em KHÔNG tự sửa: Meta đang dùng tín hiệu này để tối ưu, đổi là mất dữ liệu học.
- Mã chết: khối JS trỏ `.dia-chi` và trường `moc` đã gỡ khỏi HTML. Vô hại, để đó.

## 5. CẦN GỠ — bốn việc cũ, cả bốn cần anh Sơn
1 · Quảng cáo hẹn kết thúc 1/9, nay 13/9 — còn chạy không?
2 · PR #4 chưa merge — `main` thiếu `gia-han-quang-cao.yml` + `FB_TOKEN`
3 · 3 chiến dịch Aztlan 870.000đ/ngày — đã tắt chưa?
4 · Hàng đợi thông báo — rút tay không xuể, cần tắt bớt lệnh trùng
