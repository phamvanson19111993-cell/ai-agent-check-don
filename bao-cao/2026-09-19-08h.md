# Phòng 7 · Lady Page — 08h ngày 19/09

## 1. Giờ qua làm được gì
Gập bảng "chép nguyên nhãn phụ" lại (commit dưới). Không xả hàng đợi lệnh hẹn giờ.

## 2. Số mới nhất — KHÔNG ĐỔI so với giờ trước
Bảng đơn `modifiedTime` vẫn **2026-08-30T02:19:26Z**, 21 dòng → **20 ngày** không đơn.
Không ảnh Ads mới trong chat, không số quảng cáo mới.

## 3. CẢI TIẾN GIỜ NÀY — trang ngắn bớt 1.593px (gần 2 màn hình)
**Sửa gì:** bảng chép nguyên văn công bố/nhãn phụ (11 dòng: thành phần, phụ liệu, quy cách,
nhà sản xuất…) chuyển thành khối **gập lại**, có dòng bấm mở *"Xem bản chép nguyên văn"*.
Không xoá một chữ nào — ai muốn đọc thì bấm mở, ra đúng 11 dòng như cũ.
**Vì sao:** chặng "Tới hồ sơ → Tới bảng giá", việc 2 bảng tra — phần giấy tờ dài quá, khách
chán trước khi tới giá. **Đo bằng gì:** `ViewContent hồ sơ → ViewContent bảng giá`.
**⚠ Câu tuân thủ KHÔNG bị gập.** Khối "Lưu ý — Thực phẩm này không phải là thuốc…" em tách ra
để **nằm ngoài, luôn hiện**. Đã đo trên trình duyệt: câu này hiện ở **3 chỗ** như trước.
**Thử:** Chromium 390×844, có chụp màn hình — không lỗi JS, gập/mở đúng, không tràn ngang.
Không đụng giá, mốc combo, quà tặng, số giấy tờ (chỉ gập, không sửa số nào).

## 4. CẦN GỠ — vẫn bốn việc của anh Sơn
| | Việc |
|---|---|
| 1 | **Tắt bớt lệnh hẹn giờ** — `18 * * * *` · `30 * * * *` · `53 * * * *` cùng bắn một phiên. Em không tự tắt |
| 2 | **PR #4: bấm Merge, rồi thêm bí mật `FB_TOKEN`** |
| 3 | Quảng cáo hẹn kết thúc 1/9, nay 19/9 — còn chạy không? 3 chiến dịch Aztlan 870.000đ/ngày tắt chưa? |
| 4 | Đổi trả: **bao nhiêu ngày, ai trả phí ship về?** |
