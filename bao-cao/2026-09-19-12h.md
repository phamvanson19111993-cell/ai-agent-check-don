# BÁO CÁO — Phòng 8 · Fanpage Pancake
Kỳ: 2026-09-19 12h   ·   Nhánh: claude/fanpage-pancake-auto-update-iy8fuu

> Em bỏ các kỳ từ 14/09 đến 18/09 — lỗi của em. Đơn này gộp cả khoảng đó.

## 1. LÀM ĐƯỢC TỪ LẦN BÁO TRƯỚC
- Anh Sơn hỏi số chưa chốt mới đến 12/9 → em rà 3 nguồn (Pancake, Drive, file local), **không nguồn nào có dữ liệu sau 15/8**.
- Phát hiện sheet Drive chính đã được sửa **10/9**: không thêm dòng mới, nhưng **có ghi chú sale mới** và một tab đơn hàng.
- Viết `tools/cap_nhat_theo_sheet.py` lọc danh sách theo ghi chú mới (commit d4b2d6f).
- Giao anh Sơn 2 file: `sdt_chua_chot_cap_nhat_10-9.csv` (781 số) và `sdt_da_loai_10-9.csv` (13 số bị loại kèm lý do).

## 2. ĐANG LÀM
- Không có việc đang dở. Vẫn đứng chờ **API key Pancake** — ngày thứ 23.
- Còn tồn: file Drive lỗi `1nJJLT_Zg4UKP2UTtoshTijeEQywEe5wqCtw2zxp3hGM` chưa xoá.

## 3. DỮ LIỆU MỚI TẠO RA  (để phòng khác dùng lại)
DATA UPDATE
- Loại dữ liệu: Danh sách SĐT khách **chưa chốt — đã lọc lại**
- Giá trị: **781 số cần gọi** (giảm từ 794: bỏ 2 đã chốt, 6 không nhu cầu, 5 số hỏng); **7 số có hẹn gọi lại** xếp lên đầu
- Kỳ: dữ liệu gốc đến **15/8/2026**; lọc theo ghi chú cập nhật đến **10/9/2026**
- Nguồn: sheet "SĐT chưa chốt Pancake - 3 tháng gần nhất" (`1_Sjw…kJjk`), đọc 14/09; tab đơn hàng trong cùng sheet
- Thời gian cập nhật: 2026-09-19
- Agent tạo: Phòng 8
- Trạng thái xác minh: Suy ra, cần kiểm — căn cứ ghi chú sale nhập tay, chưa đối chiếu Pancake

**Kéo trực tiếp từ Pancake: vẫn 0 đơn.** Từ 16/8 đến 19/9: `CHƯA ĐỦ DỮ LIỆU`.
**Lịch chạy hằng ngày: CHƯA CÀI trên máy anh Sơn.**

## 4. ĐANG BỊ CHẶN — cần ai gỡ
- **Không có API key Pancake** → cần **anh Sơn** (Pancake → Cấu hình → Cấu hình ứng dụng → Webhook & API Key).
- Máy chủ em chặn mạng ra `pancake.vn`/`pages.fm` → cần **anh Sơn chạy `./caidat.sh` trên máy anh**.
- Dữ liệu gốc đã cũ **35 ngày** — Phòng Data Center và Phòng Tài chính phải biết mốc dừng là 15/8, không phải hôm nay.
- **Đề nghị chờ quyết từ 13/09**: cấp key, hoặc tạm dừng lịch báo cáo của Phòng 8.

## 5. CẦN DỮ LIỆU CỦA PHÒNG KHÁC
- Không có.

## 6. PHÁT HIỆN MÂU THUẪN
- **Mới**: 2 khách trong danh sách "chưa chốt" thực ra **đã chốt đơn** (1 đơn Nattokinase, 1 đơn Rich+DHA ngày 26/08) — tab đơn hàng có ghi, cột Tình trạng chưa sửa. Phòng nào đếm "chưa chốt" theo cột Tình trạng sẽ đếm thừa. Số cụ thể nằm trong `data/sdt_da_loai_10-9.csv` (không đưa vào git).
- **MT-14** (sheet cũ mất số 0 đầu SĐT) vẫn như cũ.
