# Phòng 7 · Lady Page — 13h ngày 19/09

## 1. Giờ qua làm được gì
Sửa nốt chỗ chữ số bị bôi đậm giả (commit dưới). Không xả hàng đợi lệnh hẹn giờ.

## 2. Số mới nhất — KHÔNG ĐỔI so với giờ trước
Bảng đơn `modifiedTime` vẫn **2026-08-30T02:19:26Z**, 21 dòng → **20 ngày** không đơn.
Không ảnh Ads mới trong chat, không số quảng cáo mới.

## 3. CẢI TIẾN GIỜ NÀY — hết chữ số nhoè
**Sửa gì:** giờ trước em phát hiện chữ mono dùng nét 600/700 ở 22 chỗ mà trang chỉ tải nét
400/500, trình duyệt phải bôi đậm giả nên nhoè. Nay xin thêm nét `600;700` cho IBM Plex Mono.
**22 chỗ đó là chỗ nào:** số công bố `4107/2024/ĐKSP` · mã chứng khoán `2927` · số điện thoại
`0913.351.394` · số tài khoản `3869 1388 888` · giá `17.340.000đ` · tiêu đề các bảng so sánh.
Toàn thứ khách phải đọc để tin và để tra — nhoè là mất điểm đúng chỗ không được mất.
**Vì sao chọn cách này, không chọn cách đổi CSS:** đổi CSS xuống nét 500 thì khỏi tải thêm,
nhưng làm nhạt đi đúng những con số cần nổi. IBM Plex Mono không có bộ chữ Việt, chỉ Latin nên
file nhẹ — đánh đổi đáng.
**Đo được:** quét cả trang, đối chiếu nét CSS dùng với nét trang xin —
trước: thiếu `IBM Plex Mono 600` và `700`; **nay: không còn nét nào dùng mà không xin.**
**Thử:** Chromium 390×844 — không lỗi JS, không tràn ngang.
*Sổ sách cân nặng: giờ 12h bỏ 1 nét chữ Việt (nặng), giờ này thêm 2 nét mono chỉ Latin (nhẹ).*

## 4. CẦN GỠ — vẫn bốn việc của anh Sơn
| | Việc |
|---|---|
| 1 | **Tắt bớt lệnh hẹn giờ** — `18 * * * *` · `30 * * * *` · `53 * * * *` cùng bắn một phiên. Em không tự tắt |
| 2 | **PR #4: bấm Merge, rồi thêm bí mật `FB_TOKEN`** |
| 3 | Quảng cáo hẹn kết thúc 1/9, nay 19/9 — còn chạy không? 3 chiến dịch Aztlan 870.000đ/ngày tắt chưa? |
| 4 | Đổi trả: **bao nhiêu ngày, ai trả phí ship về?** |
